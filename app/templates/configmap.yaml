apiVersion: v1
kind: ConfigMap
metadata:
  name: html-customer-config
  namespace: app
  labels:
    app: html-customer-app
    desafio: jackexperts
data:
  index.html: |               
    <div class="center-text">
      {{ .Values.centerText }}
    </div>
    <div class="text">
      {{ .Values.subText }}
    </div>